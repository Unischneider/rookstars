<div class="container">
  <div class="header">
    <h1>Select teams</h1>
  </div>
  <div class="team-list">
    <%= form_for [@project, @proposal] do |f| %>
      <%= f.select(:team_id, @teams.map {|team| team.id}) %>
      <%= f.submit %>
    <% end %>
  </div>
</div>

