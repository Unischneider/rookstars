<%= form_tag(team_team_members_path(team), method: :post, remote: true) do %>
  <%# hidden_field_tag :team_id, team.id %>
  <%= label_tag 'Enter the email of a Rookstar' %>
  <%= text_field_tag :email %>
  <%= submit_tag 'Add another teammate', name: nil, class: "custom-primary-btn" %>
<% end %>

